 var connection = require('./conn');
 var connection2 = require('./conn_mongo');
 var doquery = function(query , next) {
   console.log("Do query " , query, "  >>>>");
   connection.query(query, function(err, rows) {
     if (err) {
       return next(err, rows);
     }
     next(null, rows);
   });
   if (query.remove) {

   } else if (1==1) {
     var table = connection2.user;
     table.find({}, function(err, rows) {
       if (err) {
       	console.log(err);
       }
       console.log(rows);
     });
   } else if (query.add) {
   };
 };

 module.exports = doquery;